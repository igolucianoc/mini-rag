/**
 * Seed placeholder da Etapa 02.
 * A carga de dados real (documentos, chunks, embeddings) é da Etapa 03 e não
 * deve chamar LLM/embeddings aqui. Por enquanto apenas registra que rodou.
 */
function main(): void {
  // eslint-disable-next-line no-console
  console.log('seed vazio (Etapa 03)');
}

main();
